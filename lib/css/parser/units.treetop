grammar Units
  rule length_unit
    'em' / 'ex' / 'px' / 'in' / 'cm' / 'mm' / 'pt' / 'pc'
  end

  rule angle_unit
    'deg' / 'grad' / 'rad'
  end

  rule time_unit
    'ms' / 's'
  end

  rule frequency_unit
    'Hz' / 'kHz'
  end
end