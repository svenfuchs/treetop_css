grammar CSSDeclaration
  include CSSExpression
  
  rule declaration
    property s ':' s expression? s priority? s (semicolon s)*
  end
  
  rule property
    '*'? s identifier
  end
  
  rule priority
    '!important'
  end
end